import 'package:flutter/material.dart';

class GpaTab extends StatelessWidget {
  const GpaTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("GPA Calculator (UI)"));
  }
}